/**
 * `useReducedMotion` — re-exported from `lib/hooks`. Already used by the
 * CountUp component so we know the implementation is server-render safe.
 */

export { useReducedMotion } from "@/lib/hooks/use-reduced-motion"
